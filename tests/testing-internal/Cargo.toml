[package]
name = "hash-testing-internal"
version = "0.1.0"
edition = "2021"

[lib]
doctest = false

[dependencies]
itertools = { workspace = true }
quote = { workspace = true }

hash-pipeline = { workspace = true }
