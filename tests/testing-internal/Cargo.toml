[package]
name = "hash-testing-internal"
version = "0.1.0"
edition = "2021"

[lib]
doctest = false

[dependencies]
itertools = "0.10"
quote = "1.0"

hash-pipeline = { path = "../../compiler/hash-pipeline" }
