// run=pass, stage=semantic

Bar := #erase trait<T> {
    into_bar: (T) -> Self;
}


Bar := #memoise () -> u32 => {
    into_bar: (T) -> Self;
}

main := () => {
    t := #dump_ast {
        k := perform_operation()
        unmerge(k)
    }
}
