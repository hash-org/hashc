// run=pass, stage=parse

// This demonstrates that one can specify that funciton arguments have 
// default values.
foo := (l: u32, k: u32 = 2) -> u32 => {
    l * k
};


bar := (l: u32, k = 2, g = 2) -> u32 => {
    l * k
};
