// run=pass, stage=parse

// All of these patterns are refutable and will not pass the refutability check

[a, b, ...x] := foo();
[a, b] := foo();
[] := foo();

[x, Struct(name)] = *mu();

[(a, 3) | (a, 2), (c, d), (r, m)] := foo();

