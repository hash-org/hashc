// run=fail, stage=semantic


Bar := #memoise () -> u32 => {
    into_bar := (T) -> u32 => {
        2
    }
}

main := () => {
    t := #dump_ast {
        k := perform_operation()
        unmerge(k)
    }
}
