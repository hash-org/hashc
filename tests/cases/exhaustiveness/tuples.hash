// run=pass, stage=typecheck

main := () => {
    t := ("Alex", 21, 183.7);

    (name, age, height) := t;

    combo := ((age, height), name);
    
    match combo {
        ((a, f), n) => {};
    }
};
