// stage=ir, args=-C dump=ir -C ir-dump-mode=pretty, skip=true

// @@Temporary: disabled before new tc becomes default

foo := (i: i32) -> i32 => {
  match i {
    1..29 => 3,
    79..128 => i + 1,
    _ => 0
  }
}
