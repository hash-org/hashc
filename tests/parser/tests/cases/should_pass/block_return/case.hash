let compare_id = (car: Car, id: int) => {
    // destruct the 'id' out of car and rename it
    let Car {id = car_id} = car;

    // This acts as the return statement of the block
    id == car_id
};
