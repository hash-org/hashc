//! Various errors that can occur during constant evaluation. Errors include
//! things like overflow, underflow, divide by zero, other undefined behaviour
//! and so on.  

#[derive(Debug)]
pub enum ConstEvalError {}
